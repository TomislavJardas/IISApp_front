<Window x:Class="IISApp.SOAPWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IISApp"
        mc:Ignorable="d"
        Title="SOAPWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <Label Grid.Row="0" Grid.Column="0" Content="Search:" Margin="5" VerticalAlignment="Center" FontSize="14" FontWeight="Bold" />
        <TextBox x:Name="SearchTermTextBox" Grid.Row="0" Grid.Column="1" Margin="5" />


        <Button x:Name="SendSOAPRequestButton" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Send SOAP Request" Margin="5" Click="SendSOAPRequestButton_Click" />


        <TextBox x:Name="PlayersTextBox"
                 Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="5"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Window>
